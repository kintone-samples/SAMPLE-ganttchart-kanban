import type { TypeDefinition, SavedTypeDefinition } from "./typedefinitions";
import type { TsExpression } from "./expression";
export declare class Namespace implements TsExpression {
    private namespace;
    private typeDefinition;
    private savedTypeDefinition;
    constructor(namespace: string, typeDefinition: TypeDefinition, savedTypeDefinition: SavedTypeDefinition);
    tsExpression(): string;
}
