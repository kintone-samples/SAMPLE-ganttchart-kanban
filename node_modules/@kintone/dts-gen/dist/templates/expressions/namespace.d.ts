import { TypeDefinition, SavedTypeDefinition } from "./typedefinitions";
import { TsExpression } from "./expression";
export declare class Namespace implements TsExpression {
    private namespace;
    private typeDefenition;
    private savedTypeDefenition;
    constructor(namespace: string, typeDefenition: TypeDefinition, savedTypeDefenition: SavedTypeDefinition);
    tsExpression(): string;
}
