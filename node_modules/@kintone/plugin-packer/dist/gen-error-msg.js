"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateErrorMessages = void 0;
const generateErrorMessages = (errors) => {
    return errors.map((e) => {
        if (e.keyword === "enum") {
            return `"${e.dataPath}" ${e.message} (${e.params.allowedValues
                .map((v) => `"${v}"`)
                .join(", ")})`;
        }
        return `"${e.dataPath}" ${e.message}`;
    });
};
exports.generateErrorMessages = generateErrorMessages;
//# sourceMappingURL=gen-error-msg.js.map